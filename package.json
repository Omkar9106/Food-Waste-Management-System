{
  "name": "food-waste-management",
  "version": "1.0.0",
  "scripts": {
    "build": "flutter build web --release",
    "build:html": "flutter build web --release",
    "clean": "flutter clean",
    "get": "flutter pub get"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.105.4",
    "firebase": "^12.14.0"
  }
}
